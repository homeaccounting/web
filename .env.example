VITE_API_BASE_URL=http://localhost:8080
# Optional: GoatCounter count endpoint for local `pnpm dev` / `pnpm build`.
# The published image sets this at build time (see justfile / ci.yml build-arg);
# analytics is off on localhost regardless. Leave empty to disable locally.
VITE_GOATCOUNTER_URL=
